        static int[] GenerujLosowe(int n,int min, int max)
        {
            int[] tab = new int[n];
            Random r = new Random();
            for(int i = 0; i < tab.Length; i++)
            {
                tab[i]=r.Next(min,max+1);
            }
            return tab; 
        }
